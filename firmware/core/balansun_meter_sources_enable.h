#pragma once

/*
 * balansun_meter_sources_enable.h — Per-source compile switches (1 = link driver).
 * Defaults from balansun_meter_pack_enable.generated.h (see meter_pack_manifest.json).
 * Override any BALANSUN_ENABLE_SOURCE_* via PlatformIO build_flags before include.
 */

#include "balansun_meter_pack.h"
#include "balansun_meter_pack_enable.generated.h"
