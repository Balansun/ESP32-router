#pragma once

/*
 * balansun_load_channel.h — Load channel config (schedules, GPIO/HTTP, triac).
 * Implementation: Actions.h, Actions.cpp (config/wire), balansun_load_runtime.cpp (GPIO/HTTP).
 */
#include "Actions.h"

using BalansunLoadChannel = Action;
