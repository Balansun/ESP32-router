; In-tree Balansun domain library — native unit tests only.
[platformio]
default_envs = native
src_dir = src
include_dir = include
test_dir = test/native

[env:native]
platform = native
test_framework = googletest
build_flags = -std=c++17
test_build_src = yes
